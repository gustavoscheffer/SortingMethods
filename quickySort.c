#include <stdio.h>

int main(int argc, char const *argv[])
{


	/* "for" used to show sorted array */
	for (int i = 0; i < LENGTH ; i++)
	{
		printf("array[%d] => %d \n",i, arr[i]);
	}

	printf("\n *** QUICK SORT ***\n");
	printf("\n");
	/* exit */
	return 0;
}